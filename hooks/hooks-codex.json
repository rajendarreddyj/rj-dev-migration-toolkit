{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const p = process.env.COPILOT_PLUGIN_ROOT || process.env.PLUGIN_ROOT || '.'; require('child_process').execSync('node \\\"' + p + '/hooks/run-hook.js\\\" session-start-codex', { stdio: 'inherit' })\"",
            "async": false
          }
        ]
      }
    ]
  }
}
