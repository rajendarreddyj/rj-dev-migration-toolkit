{
  "name": "rj-dev-migration-toolkit",
  "version": "1.0.5",
  "description": "AI-powered migration pipeline: Java Servlet/JSP/ExtJS → Spring Boot 4.x + React. Works with GitHub Copilot, Claude Code, Antigravity, Codex, and Cursor.",
  "author": "migration-toolkit",
  "license": "MIT",
  "keywords": [
    "ai",
    "skills",
    "migration",
    "spring-boot",
    "react",
    "servlet",
    "extjs",
    "copilot",
    "claude",
    "codex",
    "cursor",
    "antigravity"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/org/rj-dev-migration-toolkit"
  },
  "scripts": {
    "build": "markdownlint --config .markdownlint.json \"**/*.md\" --ignore-path .gitignore",
    "test": "npm run lint",
    "lint": "markdownlint --config .markdownlint.json \"**/*.md\" --ignore-path .gitignore",
    "format": "prettier --write \"**/*.md\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.md\" --ignore-path .gitignore",
    "plugin:version": "node scripts/bump-plugin-version.mjs",
    "plugin:version:patch": "npm run plugin:version -- patch",
    "plugin:version:minor": "npm run plugin:version -- minor",
    "plugin:version:major": "npm run plugin:version -- major",
    "plugin:version:set": "npm run plugin:version -- set",
    "plugin:version:dry-run": "npm run plugin:version -- patch --dry-run"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.49.0",
    "prettier": "^3.5.3"
  },
  "engines": {
    "node": ">=20"
  },
  "plugin": {
    "skills": "skills/",
    "agents": "agents/",
    "prompts": "prompts/",
    "instructions": "instructions/"
  }
}
